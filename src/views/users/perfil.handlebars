<h1> Perfil do usuario: </h1>
<div class="card">
    <div class="card-body">
        <div class="container">

            {{#if user}}
                <p> Nome: {{user.name}}</p>
                <p> Email: {{user.email}} </p>
                {{!-- <p> Status: {{user.blocked}} </p> --}}
            {{else}}
                <p> Você não está logado!</p>
            {{/if}}
        </div>
    </div>
</div>