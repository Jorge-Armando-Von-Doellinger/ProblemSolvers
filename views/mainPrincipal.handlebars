
<div class="card">
    <div class="card-body">
        <h1 class="text-center" style="color: orange;"> Problems Solvers</h1>

        <p class="text-center" style="color: cornflowerblue;"> Procure e/ou publique formas de resolver seu problema. Aqui nossa comunidade estará compartilhando ideias e possíveis soluções para te ajudar!</p>

        <div class="d-flex justify-content-center align-items-center mt-5" style="height: 100vh;">
            <div class="container">
    <!-- Conteúdo do container aqui -->


        
            <h4> Problemas mais recentes: </h4>
                {{#each problems}}
                    <form action="/problems/view/{{_id}}" class="mt-4 col-md-6 mx-4" style="" method="get" style="width: 55px" class="">
            
                    <button class="p-4" style="height: 265px; width: 1000px">
                        <h3 class="text-danger"> {{ title }} </h3>
                        <hr>
                        <small> {{summary}} </small><br/>
                        <small> {{ status }} </small><br/>
                        <small> {{created_at}} </small><br/>
                    </button>

                    </form>
                    {{!-- <div class="border border-dark p-4 mt-4 col-md-6 mx-4" style="width: 550px;">
                    </div> --}}
                    
                {{else}}
                    <p> Nenhum problema registrado </p>
                {{/each}}
            </div>
        </div>
    </div>
</div>
<form action="/problems/post" method="get">
    <button type="submit" class="btn btn-success mt-2"> Poste seu problema </button>
</form>